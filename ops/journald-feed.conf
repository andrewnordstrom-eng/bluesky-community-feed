# Journald size cap for bluesky-feed VPS
# Deploy to /etc/systemd/journald.conf.d/bluesky-feed.conf
# Then: sudo systemctl restart systemd-journald
#
# Prevents journal logs from filling the disk.

[Journal]
SystemMaxUse=500M
SystemKeepFree=200M
MaxFileSec=7day
