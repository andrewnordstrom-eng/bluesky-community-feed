name: release-notes

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-notes:
    uses: andrewnordstrom-eng/.github/.github/workflows/release-notes.yml@main
    secrets: inherit
