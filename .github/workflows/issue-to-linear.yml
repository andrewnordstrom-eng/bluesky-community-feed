name: issue-to-linear

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write

jobs:
  issue-to-linear:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/issue-to-linear.yml@3c607b1f39d2cf8d6dac6029115ad99964109401
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
