name: issue-to-linear

on:
  issues:
    types: [opened, reopened]

permissions:
  contents: read
  issues: write

jobs:
  issue-to-linear:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/issue-to-linear.yml@5be6b1ba47fe3f338447eea17e6b5c465fc979f3
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
