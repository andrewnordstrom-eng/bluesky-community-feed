name: linear-state-sync

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize, ready_for_review, closed]

permissions:
  contents: read
  pull-requests: read

jobs:
  linear-state-sync:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/linear-state-sync.yml@5be6b1ba47fe3f338447eea17e6b5c465fc979f3
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
