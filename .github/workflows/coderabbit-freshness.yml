name: coderabbit-freshness

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, labeled, unlabeled]
  status:

permissions:
  contents: read
  checks: read
  statuses: read
  pull-requests: read

jobs:
  coderabbit-freshness:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/coderabbit-freshness.yml@4b5b00bce8f128fa59f8939e43043704184b9550
