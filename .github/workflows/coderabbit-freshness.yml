name: coderabbit-freshness

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, labeled, unlabeled]
  status:

permissions:
  contents: read
  checks: read
  statuses: read
  pull-requests: read

jobs:
  coderabbit-freshness:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/coderabbit-freshness.yml@71a313e8f3716d5d20e96f32226fac0f25f7a662
