name: linear-policy

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: read

jobs:
  linear-policy:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/linear-policy.yml@3c607b1f39d2cf8d6dac6029115ad99964109401
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
