name: linear-policy

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: read

jobs:
  linear-policy:
    uses: andrewnordstrom-eng/.github/.github/workflows/linear-policy.yml@main
    secrets: inherit
