name: linear-policy

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, edited]

permissions:
  contents: read
  pull-requests: read

jobs:
  linear-policy:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/linear-policy.yml@5be6b1ba47fe3f338447eea17e6b5c465fc979f3
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
