name: pr-to-linear

on:
  pull_request:
    types: [opened, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  pr-to-linear:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/pr-to-linear.yml@3eb398cf25fba2dd97a05508678fe48b0b4e0532
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
