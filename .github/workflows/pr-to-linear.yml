name: pr-to-linear

on:
  pull_request:
    types: [opened, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  pr-to-linear:
    uses: andrewnordstrom-eng/.github-public/.github/workflows/pr-to-linear.yml@5be6b1ba47fe3f338447eea17e6b5c465fc979f3
    secrets:
      LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY_PUBLIC }}
