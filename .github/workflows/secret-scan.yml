name: secret-scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  secret-scan:
    # Public repositories can only call public reusable workflows.
    uses: andrewnordstrom-eng/.github-public/.github/workflows/secret-scan.yml@5be6b1ba47fe3f338447eea17e6b5c465fc979f3
